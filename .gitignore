# ─── Secrets — NEVER commit ───
# Real EVE ESI credentials live here; they ship only baked into the built exe.
FCAT/AppSecrets.cs

# ─── Build output ───
bin/
obj/
*.user
.vs/

# ─── Rider / JetBrains ───
.idea/

# ─── NuGet ───
*.nupkg
packages/

# ─── Local AI assistant / editor settings ───
.claude/

# ─── Scratch / run logs ───
fcat_err.txt
fcat_out.txt
*.log

# ─── Packaged release archives (upload these to GitHub Releases, don't commit) ───
*.zip

# ─── Velopack build output (publish folder + generated installers/release assets) ───
publish/
Releases/

# ─── Python (the Alliance Auth connector in aa-connector/) ───
__pycache__/
*.py[cod]
*.egg-info/
.venv/
aa-connector/dist/
aa-connector/build/

# ─── Connector dev harness (local only, not part of the shipped plugin) ───
aa-connector/dev/
aa-connector/DEVELOPING.md
aa-connector/.gitattributes
